# 服务器配置
PORT=3000
NODE_ENV=development

# 数据库连接字符串（必填）
# 格式: postgresql://用户名:密码@主机:端口/数据库名
DATABASE_URL=postgresql://postgres:your_password@localhost:5432/test_db

# 云服务示例（如 Render、Heroku、Railway）:
# DATABASE_URL=postgresql://user:password@host.example.com:5432/dbname
# NODE_ENV=production  # 生产环境会自动启用 SSL
