# 三环境架构配置指南

## 架构概览

```
┌─────────────────────────────────────────────────────────┐
│  生产环境 erp.xianfeng-eu.com                            │
│  Vercel → sysafari-logistics-api → PostgreSQL (生产)     │
│  用途：日常运营，真实业务数据                              │
│  数据：基础数据 + 管理员账号，业务数据从零开始              │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  演示环境 demo.xianfeng-eu.com                           │
│  Vercel → sysafari-logistics-demo-api → PostgreSQL (演示) │
│  用途：客户展示，测试演示                                  │
│  数据：完整的测试数据                                     │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  本地开发 localhost                                      │
│  :5173 → :3001 → PostgreSQL (本地 Mac)                  │
│  用途：开发调试，不上传到 GitHub                          │
└─────────────────────────────────────────────────────────┘
```

## 核心原则

1. **生产数据库** = Render 生产 PostgreSQL，只存储基础数据和管理员账号，业务数据从零开始
2. **演示数据库** = Render 演示 PostgreSQL，保留所有测试数据，用于演示
3. **本地数据库** = 本地 Mac 上的 PostgreSQL，仅用于开发调试
4. **数据安全** = 本地代码上传时不携带任何数据库数据

## 数据库信息

| 类型 | 数据库名称 | Render ID | 用途 | 数据内容 |
|------|-----------|-----------|------|---------|
| 生产数据库 | logistics-production-db | dpg-d4vmk8pr0fns739omrrg-a | 生产环境 | 基础数据 + 管理员 |
| 演示数据库 | PostgreSQL | dpg-d4t44e56ubrc73ectgq0-a | 演示环境 | 全部测试数据 |
| 本地数据库 | sysafari_logistics | 本地 PostgreSQL | 开发测试 | 开发数据 |

## API 服务信息

| 环境 | 服务名称 | URL | Render ID | 连接数据库 |
|------|----------|-----|-----------|-----------|
| 生产 | sysafari-logistics-api | https://sysafari-logistics-api.onrender.com | srv-d4v9kg8gjchc73co09s0 | 生产数据库 |
| 演示 | sysafari-logistics-demo-api | https://sysafari-logistics-demo-api.onrender.com | srv-d4vd44qli9vc73dm4fkg | 演示数据库 |

## 基础数据表清单（迁移到生产数据库）

| 表名 | 说明 |
|-----|------|
| shipping_companies | 船公司 |
| container_codes | 集装箱代码 |
| ports_of_loading | 装货港 |
| destination_ports | 目的港 |
| countries | 国家 |
| air_ports | 机场 |
| transport_methods | 运输方式 |
| vat_rates | 增值税率 |
| service_fee_categories | 服务费类别 |
| service_fees | 服务费 |
| tariff_rates | 关税税率 |
| roles | 角色 |
| permissions | 权限 |
| role_permissions | 角色权限关联 |

**不迁移的表（在生产数据库中为空）：**
- bills_of_lading（提单）
- customers（客户）
- suppliers（供应商）
- fees（费用）
- 其他业务数据表

---

## 配置步骤

### 步骤 1: 获取数据库连接字符串

1. 登录 [Render Dashboard](https://dashboard.render.com)
2. 进入 **PostgreSQL** 数据库列表
3. 分别获取两个数据库的 **External Database URL**:
   - **logistics-production-db** → 用于生产环境（生产数据库）
   - **PostgreSQL** → 用于演示环境（演示数据库）

连接字符串格式: `postgresql://用户:密码@主机/数据库名?sslmode=require`

### 步骤 2: 初始化生产数据库

在本地终端执行:

```bash
cd /Users/fengzheng/sysafari-logistics

# 设置环境变量（替换为实际连接字符串）
export SOURCE_DATABASE_URL="演示数据库的连接字符串"
export TARGET_DATABASE_URL="生产数据库的连接字符串"

# 运行初始化脚本
node server/scripts/init-production-db.js
```

脚本会自动:
- 创建所有数据库表
- 从演示数据库迁移基础数据（船公司、港口、国家等）
- 创建管理员账号 (admin / admin123)
- 业务数据表保持为空

### 步骤 3: 配置生产 API 环境变量

1. 打开 Render Dashboard
2. 进入 **sysafari-logistics-api** 服务
3. 点击 **Environment** 标签
4. 更新环境变量:
   - `DATABASE_URL` = **生产数据库**的连接字符串
   - `NODE_ENV` = production
5. 保存并等待自动重新部署

### 步骤 4: 配置演示 API 环境变量

1. 打开 Render Dashboard
2. 进入 **sysafari-logistics-demo-api** 服务
3. 点击 **Environment** 标签
4. 确认环境变量:
   - `DATABASE_URL` = **演示数据库**的连接字符串
   - `NODE_ENV` = production
5. 取消暂停状态，等待重新部署

### 步骤 5: 配置演示前端 (Vercel)

使用 `vercel.demo.json` 配置文件部署演示环境：

```bash
cd /Users/fengzheng/sysafari-logistics
npx vercel --name sysafari-logistics-demo
```

然后在 Vercel Dashboard 绑定域名 `demo.xianfeng-eu.com`

### 步骤 6: DNS 配置

在域名管理后台添加:
- `demo.xianfeng-eu.com` → CNAME → `cname.vercel-dns.com`

---

## 本地开发环境

### 安装 PostgreSQL

```bash
# macOS 使用 Homebrew 安装
brew install postgresql@16
brew services start postgresql@16

# 创建数据库
createdb sysafari_logistics
```

### 配置环境变量

编辑 `server/.env` 文件：

```env
NODE_ENV=development
DATABASE_URL=postgresql://localhost:5432/sysafari_logistics
```

### 安装依赖

```bash
# 安装前端依赖
cd /Users/fengzheng/sysafari-logistics
npm install

# 安装后端依赖
cd server
npm install
```

### 初始化本地数据库

```bash
cd /Users/fengzheng/sysafari-logistics/server
npm run init-db
```

### 启动开发环境

```bash
# 方式一：分别启动
cd /Users/fengzheng/sysafari-logistics/server && npm run dev  # 后端
cd /Users/fengzheng/sysafari-logistics && npm run dev         # 前端

# 方式二：同时启动
cd /Users/fengzheng/sysafari-logistics && npm run dev:all
```

### 本地访问

- 前端: http://localhost:5173
- 后端: http://localhost:3001
- 账号: admin / admin123

---

## 代码上传安全

`.gitignore` 已配置以下规则，确保敏感数据不会上传到 GitHub：

```
# Environment
.env
.env.local
.env.*.local

# Database backups
backups/
```

**上传代码前请确认：**
- 运行 `git status` 确认没有敏感文件被暂存
- `.env` 文件不在 git 跟踪中

---

## 验证清单

### 本地开发环境
- [ ] 本地 PostgreSQL 服务运行中
- [ ] 后端启动时显示 `🌐 PostgreSQL 数据库连接已建立 (本地环境)`
- [ ] http://localhost:5173 可访问
- [ ] 使用 admin / admin123 可登录
- [ ] `git status` 不显示 .env 文件

### 演示环境（演示数据库）
- [ ] demo.xianfeng-eu.com 可访问
- [ ] 测试数据正常显示
- [ ] 数据操作正常

### 生产环境（生产数据库）
- [ ] erp.xianfeng-eu.com 可访问
- [ ] 使用 admin / admin123 可登录
- [ ] 基础数据（船公司、港口等）正常显示
- [ ] 业务数据为空，等待真实数据录入

### 数据隔离验证
- [ ] 演示环境和生产环境的数据相互独立
- [ ] 本地创建的数据不会出现在云端

---

## 登录信息

| 环境 | URL | 账号 | 密码 | 说明 |
|------|-----|------|------|------|
| 生产 | erp.xianfeng-eu.com | admin | admin123 | 生产数据库管理员 |
| 演示 | demo.xianfeng-eu.com | demo_admin | demo123 | 演示数据库演示账号 |
| 演示 | demo.xianfeng-eu.com | demo_operator | demo123 | 演示数据库演示账号 |
| 开发 | localhost:5173 | admin | admin123 | 本地开发账号 |

---

## 安全提醒

1. **数据库连接字符串** 属于敏感信息，仅在 Render Dashboard 中配置，绝不提交到代码仓库
2. **生产数据库操作** 需要谨慎，建议先在演示环境测试
3. **本地代码上传前** 运行 `git status` 确认没有敏感文件
4. **生产环境** 只连接生产数据库，任何测试数据都不应进入生产环境
